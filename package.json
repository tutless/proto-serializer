{
  "name": "@tutless/proto-serializer",
  "version": "1.0.1",
  "description": "sharing package across nestjs and non nestjs framework requires mongoose document inputs compromise protobuf data structure",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "author": "tutless",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "rxjs": "^7.8.2"
  }
}
